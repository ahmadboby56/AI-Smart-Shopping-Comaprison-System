services:
  - type: web
    name: smart-shopping
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn application:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
